version: "3.8"

services:

    nupic_predictor:
        image: registry.cybertron.ninja/nupic_predictor:1.0
        build:
            context: ./
            dockerfile: Dockerfile
            args:
                NUPIC_MODEL_SAVE_DIRECTORY: "/srv/app/src/nupredictor/saved_models"



